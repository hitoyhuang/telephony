--- a/src/at_response.c
+++ b/src/at_response.c
@@ +202,6 +202,10 @@
   if (strstr(response, "LTE")) {
     pvt->network_mode = NETWORK_MODE_LTE;
   }
+  // 添加 5G 识别
+  else if (strstr(response, "NR5G-SA") || strstr(response, "NR5G-NSA")) {
+    pvt->network_mode = NETWORK_MODE_5G;  // 自定义 5G 状态码
+  }
 }
